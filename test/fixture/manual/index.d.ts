/**
 * Sum (manual).
 *
 * @param a
 *   Left (manual).
 * @param b
 *   Right (manual).
 * @returns
 *   Sum (manual).
 */
export function sum(a: number, b: number): number

/**
 * Some interface.
 */
export interface SomeInterface {
  /**
   * Some property.
   */
  someProperty: string
}

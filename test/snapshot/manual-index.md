### `SomeInterface`

Some interface.

###### Fields

* `someProperty` (`string`)
  — some property

### `sum(a, b)`

Sum (manual).

###### Parameters

* `a` (`number`)
  — left (manual)
* `b` (`number`)
  — right (manual)

###### Returns

Sum (manual) (`number`).
